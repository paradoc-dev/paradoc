---
title: search
description: Search for artifacts in registries
ogTitle: paradoc search
ogDescription: Discover Paradoc artifacts across registries
---

Searches for artifacts in a registry.

## Usage

```bash
paradoc search [query] [options]
```

## Arguments

| Argument | Description |
|----------|-------------|
| `query` | Search query (matches name, title, or description) |

## Options

| Option | Description |
|--------|-------------|
| `--registry <namespace>` | Registry namespace to search (default: the only configured registry) |
| `--kind <kind>` | Filter by artifact kind: `form`, `document`, `checklist`, `bundle` |
| `--tags <tags>` | Filter by tags (comma-separated) |
| `--json` | Output as JSON |

## Description

The `search` command queries a registry's index to find matching artifacts. You can filter results by kind, tags, or a text query that matches against artifact names, titles, and descriptions.

There is no default registry. Without `--registry`, it searches the one registry you have configured. With no registry configured, it fails and tells you to pass `--registry` or run [`paradoc registry add`](/cli/commands/registry/add). With several configured, it fails and lists them so you can pick one with `--registry`.

A namespace must be configured first with `paradoc registry add`; an unconfigured one fails and prints the command to run. `@paradoc` is reserved for the Paradoc registry, which is not available yet. When the registry host cannot be reached, the command fails and names it.

## Examples

Search all artifacts:

```bash
paradoc search
```

Search with a query:

```bash
paradoc search "lease agreement"
```

Filter by kind:

```bash
paradoc search --kind form
```

Filter by tags:

```bash
paradoc search --tags legal,real-estate
```

Search a specific registry:

```bash
paradoc search --registry @acme "tax"
```

Output as JSON:

```bash
paradoc search --json
```

## Output

```
Found 3 artifacts:

@acme/residential-lease v1.0.0
  Residential Lease Agreement
  Standard residential lease agreement for property rentals
  form · #legal #real-estate · 2 layers

@acme/commercial-lease v1.0.0
  Commercial Lease Agreement
  Commercial property lease agreement
  form · #legal #commercial · 2 layers

@acme/lease-addendum v1.0.0
  Lease Addendum
  form · #legal · 1 layer

Run 'paradoc add @acme/<name>' to install an artifact.
```

## See also

- [add](/cli/commands/add) - Install an artifact
- [registry](/cli/commands/registry) - Manage registry configurations

Use `--no-cache` to bypass cached registry search results.
