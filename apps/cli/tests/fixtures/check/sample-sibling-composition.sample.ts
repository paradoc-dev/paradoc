import type { DocumentData } from '@paradoc/react'

/** Discovered by the CLI's sibling `*.sample.ts` convention. */
const sample: DocumentData = {
  fields: { lineItems: [{ description: 'Discovery workshop' }] },
  parties: {},
}

export default sample
