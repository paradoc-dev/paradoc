# @paradoc/core

## 0.2.0

- **Breaking**: Form artifact builders and `form-payload` inference now produce/consume the `EnumOption` shape for enum and multiselect fields.
- Plumb `language` through bundle, checklist, document, and form artifact types.
- Add `code` accessor on shared artifact methods.

## 0.1.1

Support `capacity` and `printed_name` signature blocks.

## 0.1.0

Initial public release.
